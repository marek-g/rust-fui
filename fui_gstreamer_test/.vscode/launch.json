{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "request": "launch",
            "type": "lldb",
            "sourceLanguages": ["rust"],
            "program": "${workspaceRoot}/target/debug/fui_gstreamer_test",
            //"type": "gdb",
            //"target": "${workspaceRoot}/target/debug/fui_gstreamer_test.exe",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build"
        }
    ]
}
